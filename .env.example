# Copy to .env and adjust. Local docker-compose default shown.
# For Supabase, use the connection string from Project Settings -> Database
# (use the session/pooler URL and add ?sslmode=require).
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/demarco
